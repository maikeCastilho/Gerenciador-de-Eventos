@model IEnumerable<Reservas.Models.Local>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Cidade)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Bairro)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Acessibilidade)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EstacionamentoDisponivel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cep)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Cidade)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Bairro)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Acessibilidade)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EstacionamentoDisponivel)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cep)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdLocal">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdLocal">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdLocal">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
